$(function() { //on DOM ready 
    		$("#scroller").simplyScroll({
				auto:true,
				frameRate: 50,
				speed: 2
			});
	});
	
$(function() { //on DOM ready 
    		$("#scroller_2").simplyScrollProject({
				auto:true,
				frameRate: 50,
				speed: 2
			});
	});	